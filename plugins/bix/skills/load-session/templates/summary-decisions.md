Extract every decision, trade-off, and architectural choice made in this conversation. Ignore implementation details, tool outputs, and chatter — focus only on what was decided and why.

Structure your output as:

**Decisions Made:**

For each decision, use this format:
- **[Decision]:** <what was decided>
  - **Why:** <the reasoning or constraint>
  - **Alternatives rejected:** <what was considered but not chosen, if discussed>

**Trade-offs Acknowledged:**
- List any explicitly discussed trade-offs (e.g., "chose X over Y because Z")

**Constraints Identified:**
- List any technical, business, or design constraints discovered during the conversation

**Deferred Decisions:**
- List anything explicitly postponed for later (or "None")

Rules:
- Only include decisions that were actually made or confirmed — not suggestions that were ignored
- Include the reasoning (why), not just the what
- If a decision was reversed during the conversation, note the final decision and that it was changed
- Be concise — one line per decision unless the reasoning is complex